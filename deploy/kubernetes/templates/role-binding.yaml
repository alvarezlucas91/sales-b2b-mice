apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: pod-creator-binding
  namespace: "${eks_namespace}"
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: "${eks_namespace}e-pod-launcher-role"
subjects:
  - kind: ServiceAccount
    name: airflow-worker
    namespace: airflow
